# Magpie hosted default relay — pointer file.
#
# The Magpie MCP fetches this file at startup and uses the first ws://|wss://
# line below as the default relay, UNLESS the user set MAGPIE_RELAY_URL.
# Migrating the relay is a one-line edit here — no re-release, no client change.
#
# Lines starting with '#' are ignored. Until an active URL line is present,
# agents run in invite-only mode (joiners paste a full CODE@ws://… invite).
#
# Current hosted default (Fly.io, nrt/Tokyo, DoS-hardened relay):
wss://magpie-relay.fly.dev
